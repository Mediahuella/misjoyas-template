{%- liquid
  assign featured_collection = section.settings.collection
  assign products_limit = section.settings.products_to_show
  assign columns_mobile = 2
  assign columns_desktop = section.settings.columns_desktop
  assign gap_mobile = section.settings.gap_mobile
  assign gap_desktop = section.settings.gap

  assign enable_slider = false
  if featured_collection != blank
    assign slider_slide_count = featured_collection.products_count
    if slider_slide_count > products_limit
      assign slider_slide_count = products_limit
    endif
    if slider_slide_count > columns_mobile
      assign enable_slider = true
    endif
  endif

  assign disable_quickview = true
  if section.settings.show_quickview
    assign disable_quickview = false
  endif
-%}

{%- style -%}
  .featured-collection-mj-{{ section.id }} {
    --fc-mj-gap: {{ gap_desktop }}px;
    --fc-mj-gap-mobile: {{ gap_mobile }}px;
    width: 100%;
    padding-top: {{ section.settings.padding_y_mobile }}px;
    padding-bottom: {{ section.settings.padding_y_mobile }}px;
  }
  @media (min-width: 768px) {
    .featured-collection-mj-{{ section.id }} {
      padding-top: {{ section.settings.padding_y }}px;
      padding-bottom: {{ section.settings.padding_y }}px;
    }
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__heading {
    margin: 0 0 1.875rem;
    text-align: center;
    color: #222222;
    font-size: 1.875rem;
    line-height: 2.25rem;
    font-weight: 400;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__heading-icon {
    color: {{ section.settings.title_icon_color }};
    margin-right: 0.25em;
  }
  @media (min-width: 768px) {
    .featured-collection-mj-{{ section.id }} .featured-collection-mj__heading {
      font-size: 2.25rem;
      line-height: 2.625rem;
      margin-bottom: 2rem;
    }
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__track {
    padding-left: 1rem;
    padding-right: 1rem;
  }
  @media (min-width: 768px) {
    .featured-collection-mj-{{ section.id }} .featured-collection-mj__track {
      padding-left: 0;
      padding-right: 0;
    }
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__slider {
    position: relative;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__carousel {
    position: relative;
  }
  @media (max-width: 767px) {
    .featured-collection-mj-{{ section.id }} .preload-slide-{{ section.id }} {
      {%- if settings.lang_direction contains request.locale.iso_code -%}
        margin-left: {{ gap_mobile }}px;
      {%- else -%}
        margin-right: {{ gap_mobile }}px;
      {%- endif -%}
      width: calc(((100% + {{ gap_mobile }}px) / {{ columns_mobile }}) - {{ gap_mobile }}px);
    }
  }
  @media (min-width: 768px) {
    .featured-collection-mj-{{ section.id }} .preload-slide-{{ section.id }} {
      {%- if settings.lang_direction contains request.locale.iso_code -%}
        margin-left: {{ gap_desktop }}px;
      {%- else -%}
        margin-right: {{ gap_desktop }}px;
      {%- endif -%}
      width: calc(((100% + {{ gap_desktop }}px) / {{ columns_desktop }}) - {{ gap_desktop }}px);
    }
  }
  .featured-collection-mj-{{ section.id }} .splide__arrows {
    display: none;
  }
  @media (min-width: 768px) {
    .featured-collection-mj-{{ section.id }} .splide__arrows {
      display: block;
      position: absolute;
      inset: 0;
      pointer-events: none;
      z-index: 30;
    }
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__progress {
    display: flex;
    align-items: center;
    width: 100%;
    margin: 1.25rem 0 0;
    padding: 0 1rem;
    box-sizing: border-box;
  }
  @media (min-width: 768px) {
    .featured-collection-mj-{{ section.id }} .featured-collection-mj__progress {
      display: none !important;
    }
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__progress .splide-progress {
    display: block;
    width: 100%;
    min-height: 3px;
    overflow: hidden;
    border-radius: 4px;
    background: rgba(34, 34, 34, 0.12);
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__progress .splide-progress-bar {
    display: block;
    height: 3px;
    max-width: 100%;
    border-radius: 4px;
    background: #222222;
    margin: 0;
    transform: none;
    transition: width 0.25s ease;
    will-change: width;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__arrow {
    pointer-events: auto;
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    align-items: center;
    justify-content: center;
    box-sizing: border-box;
    width: 28px;
    min-height: 44px;
    padding: 10px 2px;
    border: none;
    background: rgba(255, 255, 255, 0.9);
    color: #222222;
    box-shadow: none;
    cursor: pointer;
    transition: background-color 0.15s ease, opacity 0.15s ease;
  }
  .featured-collection-mj-{{ section.id }} .x-splide:hover .featured-collection-mj__arrow {
    transform: translateY(-50%);
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__arrow:hover,
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__arrow:focus,
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__arrow:focus-visible {
    background: rgba(255, 255, 255, 1);
    color: #222222;
    box-shadow: none;
    opacity: 1;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__arrow:disabled {
    opacity: 0.4;
    cursor: not-allowed;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__arrow--prev {
    left: 0;
    border-radius: 0 6px 6px 0;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__arrow--next {
    right: 0;
    border-radius: 6px 0 0 6px;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__arrow-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    flex-shrink: 0;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__arrow-icon svg {
    width: 24px;
    height: 24px;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__view-all {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-top: 1.875rem;
    min-height: 3.125rem;
    padding: 0.625rem 1.5rem;
    border: 1px solid #222222;
    border-radius: 4px;
    color: #222222;
    font-size: 1rem;
    line-height: 1.375rem;
    text-transform: uppercase;
    text-decoration: none;
    text-align: center;
    max-width: 21.25rem;
    width: calc(100% - 2.5rem);
    background-color: transparent;
    transition: background-color 0.15s ease;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__view-all:hover,
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__view-all:focus-visible {
    background-color: #f2f2f2;
  }
  .featured-collection-mj-{{ section.id }} .featured-collection-mj__footer {
    display: flex;
    justify-content: center;
    padding: 0 1.25rem;
  }
{%- endstyle -%}

<div
  class="featured-collection-mj-{{ section.id }} tw-w-full"
  role="region"
  aria-label="{{ 'sections.featured-collection-misjoyas.aria_label' | t }}"
>
  <div class="x-page-width page-width">
    {%- if section.settings.title != blank -%}
      <h2 class="featured-collection-mj__heading">
        {%- if section.settings.title_icon != blank -%}
          <span class="featured-collection-mj__heading-icon" aria-hidden="true">{{ section.settings.title_icon | escape }}</span>
        {%- endif -%}
        {{- section.settings.title | escape -}}
      </h2>
    {%- endif -%}

    {%- if featured_collection == blank -%}
      {%- if request.design_mode -%}
        <p class="tw-text-center tw-text-sm tw-text-[#737373]">
          {{ 'sections.featured-collection-misjoyas.placeholder' | t }}
        </p>
      {%- endif -%}
    {%- else -%}
      <div class="featured-collection-mj__slider">
      <div
        id="featured-collection-mj-{{ section.id }}"
        class="featured-collection-mj__carousel carousel-mobile carousel-tablet overflow-hidden x-splide splide visible disable-effect-hover{% unless enable_slider %} featured-collection-mj__carousel--static{% endunless %}"
        {% if enable_slider %}
          x-data-slider="{{ section.id }}"
          x-intersect.once.margin.200px='$store.xSplide.load($el, {
            "speed": 1000,
            "type": "slide",
            "mediaQuery": "min",
            "updateOnMove": "true",
            "gap": "{{ gap_mobile }}px",
            "perPage": {{ columns_mobile }},
            "perMove": {{ columns_mobile }},
            "autoWidth": false,
            "arrows": false,
            "pagination": false,
            "drag": true,
            "trimSpace": false,
            {%- if settings.lang_direction contains request.locale.iso_code -%}
              "direction": "rtl",
            {%- endif -%}
            "breakpoints": {
              300: {
                "gap": "{{ gap_mobile }}px",
                "perPage": {{ columns_mobile }},
                "perMove": {{ columns_mobile }}
              },
              768: {
                "destroy": false,
                "perPage": {{ columns_desktop }},
                "perMove": {{ columns_desktop }},
                "autoWidth": false,
                "gap": "{{ gap_desktop }}px",
                "drag": true
              },
              1024: {
                "perPage": {{ columns_desktop }},
                "perMove": {{ columns_desktop }},
                "gap": "{{ gap_desktop }}px"
              }
            },
            "classes": {
              "arrows": "splide__arrows",
              "pagination": "hidden"
            },
            "events": [
              {
                "event": "ready",
                "callback": function () {
                  document.dispatchEvent(new CustomEvent("eurus:{{ section.id }}:splide-ready"));
                }
              },
              {
                "event": "mounted resized",
                "callback": function () {
                  if (window.bindFeaturedCollectionMjProgress) {
                    window.bindFeaturedCollectionMjProgress("{{ section.id }}");
                  }
                  if (window.bindFeaturedCollectionMjArrows) {
                    window.bindFeaturedCollectionMjArrows(
                      document.getElementById("featured-collection-mj-{{ section.id }}"),
                      {{ columns_desktop }},
                      {{ columns_mobile }}
                    );
                  }
                }
              }
            ]
          })'
        {% endif %}
      >
        <div class="splide__track featured-collection-mj__track overflow-hidden w-full{% if enable_slider %} md:cursor-grab{% endif %}">
          <div class="splide__list md:grid-cols-none md:flex md:w-full md:gap-0 flex w-full">
            {%- for product in featured_collection.products limit: products_limit -%}
              {%- liquid
                assign fc_mj_class_slide = 'splide__slide x-splide-slide preload-slide-' | append: section.id | append: ' h-auto'
              -%}
              {% render 'card-product',
                card_product: product,
                show_vendor: section.settings.show_vendor,
                show_rating: section.settings.show_rating,
                show_highlighted_attributes: section.settings.show_highlighted_attributes,
                section_id: section.id,
                class_slide: fc_mj_class_slide,
                column_mobile: columns_mobile,
                columns_desktop: columns_desktop,
                index: forloop.index,
                list_name: featured_collection.title,
                collection: featured_collection,
                block_id: section.id,
                product_card_id: forloop.index,
                disable_quickview: disable_quickview,
                media_aspect_ratio: '100',
                swiper_on_mobile: enable_slider,
                enable_desktop_slider: enable_slider,
                animation_loading: true
              %}
            {%- else -%}
              {%- if request.design_mode -%}
                <p class="tw-text-center tw-text-sm tw-p-8">
                  {{ 'sections.featured-collection-misjoyas.empty_collection' | t }}
                </p>
              {%- endif -%}
            {%- endfor -%}
          </div>
        </div>
        {%- if enable_slider -%}
          <div class="splide__arrows{% if settings.lang_direction contains request.locale.iso_code %} rtl{% endif %}">
            <button
              type="button"
              data-fc-mj-arrow="prev"
              class="featured-collection-mj__arrow featured-collection-mj__arrow--prev splide__arrow splide__arrow--prev none_border"
              aria-label="{{ 'sections.featured-collection-misjoyas.previous' | t }}"
            >
              <span class="featured-collection-mj__arrow-icon rotate-90 rtl:-rotate-90" aria-hidden="true">
                {% render 'icon-alls', icon: 'icon-caret' %}
              </span>
            </button>
            <button
              type="button"
              data-fc-mj-arrow="next"
              class="featured-collection-mj__arrow featured-collection-mj__arrow--next splide__arrow splide__arrow--next none_border"
              aria-label="{{ 'sections.featured-collection-misjoyas.next' | t }}"
            >
              <span class="featured-collection-mj__arrow-icon -rotate-90 rtl:rotate-90" aria-hidden="true">
                {% render 'icon-alls', icon: 'icon-caret' %}
              </span>
            </button>
          </div>
        {%- endif -%}
      </div>
      {%- if enable_slider -%}
        {%- liquid
          assign progress_bar_count = slider_slide_count
          assign slide_count_f = progress_bar_count | times: 1.0
          assign mobile_width_bar = columns_mobile | times: 1.0 | divided_by: slide_count_f
          if mobile_width_bar > 1.0
            assign mobile_width_bar = 1.0
          endif
        -%}
        {%- style -%}
          #featured-collection-mj-progress-{{ section.id }} .splide-progress-bar {
            width: {{ mobile_width_bar | times: 100 }}%;
          }
        {%- endstyle -%}
        <div
          id="featured-collection-mj-progress-{{ section.id }}"
          class="featured-collection-mj__progress"
        >
          <div class="splide-progress rounded-md">
            <div
              class="splide-progress-bar-{{ section.id }} splide-progress-bar rounded-md"
              data-thumb-pct="{{ mobile_width_bar | times: 100 }}"
            ></div>
          </div>
        </div>
      {%- endif -%}
      </div>

      {%- liquid
        assign view_all_url = featured_collection.url
        if section.settings.button_link != blank
          assign view_all_url = section.settings.button_link
        endif
      -%}
      {%- if section.settings.show_view_all and section.settings.button_label != blank -%}
        <div class="featured-collection-mj__footer">
          <a
            href="{{ view_all_url }}"
            class="featured-collection-mj__view-all"
            {% if section.settings.open_new_window %}target="_blank" rel="noopener noreferrer"{% endif %}
          >
            {{ section.settings.button_label | escape }}
          </a>
        </div>
      {%- endif -%}
    {%- endif -%}
  </div>
</div>

{%- if enable_slider and featured_collection != blank -%}
  <script>
    window.bindFeaturedCollectionMjProgress = window.bindFeaturedCollectionMjProgress || function (sectionId) {
      var root = document.getElementById('featured-collection-mj-' + sectionId);
      var wrap = document.getElementById('featured-collection-mj-progress-' + sectionId);
      if (!root || !root.splide || !wrap) return false;
      if (root.dataset.fcMjProgressBound === '1') return true;

      var splide = root.splide;
      var bar = wrap.querySelector('.splide-progress-bar');
      if (!bar) return false;

      root.dataset.fcMjProgressBound = '1';
      var thumbPct = parseFloat(bar.dataset.thumbPct) || 25;
      var rafId = null;

      function applyWidth(animate) {
        if (window.innerWidth >= 768) return;
        var end = splide.Components.Controller.getEnd();
        var index = Math.max(0, Math.min(splide.index, end));
        var width = end <= 0 ? thumbPct : thumbPct + (index / end) * (100 - thumbPct);
        bar.style.marginLeft = '0';
        bar.style.marginRight = '0';
        bar.style.transform = 'none';
        bar.style.width = width + '%';
        bar.style.transition = animate === false ? 'none' : 'width 0.25s ease';
      }

      function schedule(animate) {
        if (rafId) cancelAnimationFrame(rafId);
        rafId = requestAnimationFrame(function () {
          rafId = null;
          applyWidth(animate);
        });
      }

      splide.on('mounted', function () { schedule(true); });
      splide.on('move', function () { schedule(true); });
      splide.on('moved', function () { schedule(true); });
      splide.on('scroll', function () { schedule(false); });
      splide.on('dragged', function () { schedule(true); });
      splide.on('resized', function () { schedule(true); });
      splide.on('refresh', function () { schedule(true); });

      schedule(true);
      return true;
    };

    (function () {
      var sid = '{{ section.id }}';
      var desktopMove = {{ columns_desktop }};
      var mobileMove = {{ columns_mobile }};

      function bindProgress() {
        if (window.bindFeaturedCollectionMjProgress) {
          return window.bindFeaturedCollectionMjProgress(sid);
        }
        return false;
      }

      function bindArrows() {
        if (typeof window.bindFeaturedCollectionMjArrows !== 'function') return false;
        var root = document.getElementById('featured-collection-mj-' + sid);
        if (!root || !root.splide) return false;
        window.bindFeaturedCollectionMjArrows(root, desktopMove, mobileMove);
        return true;
      }

      function tick() {
        bindProgress();
        bindArrows();
      }

      function resetBindings() {
        var root = document.getElementById('featured-collection-mj-' + sid);
        if (root) delete root.dataset.fcMjProgressBound;
      }

      var attempts = 0;
      var timer = setInterval(function () {
        tick();
        var root = document.getElementById('featured-collection-mj-' + sid);
        if ((root && root.splide) || ++attempts > 150) clearInterval(timer);
      }, 100);

      document.addEventListener('shopify:section:load', function (event) {
        if (event.target && event.target.querySelector('#featured-collection-mj-' + sid)) {
          resetBindings();
          setTimeout(tick, 300);
        }
      });
    })();
  </script>
{%- endif -%}

{% schema %}
{
  "name": "Colección destacada",
  "tag": "section",
  "class": "section-featured-collection-misjoyas",
  "settings": [
    {
      "type": "text",
      "id": "title_icon",
      "label": "Icono del título",
      "default": "✦",
      "info": "Símbolo decorativo antes del título (ej. ✦)."
    },
    {
      "type": "color",
      "id": "title_icon_color",
      "label": "Color del icono del título",
      "default": "#68B5A3"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Título",
      "default": "¡Lo último!"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Colección"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "label": "Productos a mostrar",
      "min": 4,
      "max": 16,
      "step": 1,
      "default": 8
    },
    {
      "type": "range",
      "id": "gap",
      "label": "Espacio entre tarjetas — escritorio (px)",
      "min": 8,
      "max": 32,
      "step": 2,
      "default": 16
    },
    {
      "type": "range",
      "id": "gap_mobile",
      "label": "Espacio entre tarjetas — móvil (px)",
      "min": 4,
      "max": 24,
      "step": 2,
      "default": 12
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "label": "Productos visibles — escritorio",
      "min": 2,
      "max": 5,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "padding_y",
      "label": "Padding vertical — escritorio (px)",
      "min": 0,
      "max": 80,
      "step": 2,
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_y_mobile",
      "label": "Padding vertical — móvil (px)",
      "min": 0,
      "max": 80,
      "step": 2,
      "default": 30
    },
    {
      "type": "checkbox",
      "id": "show_quickview",
      "label": "Mostrar botón añadir al carro en tarjeta",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_vendor",
      "label": "Mostrar marca",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_rating",
      "label": "Mostrar valoraciones",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_highlighted_attributes",
      "label": "Mostrar atributos destacados",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Mostrar botón ver todos",
      "default": true
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Texto del botón",
      "default": "Ver todos los productos en cyber"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Enlace del botón",
      "info": "Vacío = URL de la colección."
    },
    {
      "type": "checkbox",
      "id": "open_new_window",
      "label": "Abrir enlace en nueva pestaña",
      "default": false
    }
  ],
  "presets": [
    {
      "name": "Colección destacada"
    }
  ]
}
{% endschema %}
